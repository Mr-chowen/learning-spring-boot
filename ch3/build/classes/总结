动态代理：
	1、JDK动态代理是java.lang.reflect.*包提供的方法，他必须借助一个接口才能产生代理对象。因此对于使用业务接口的类，Spring默认使用jdk动态代理实现AOP
	2、CGLIB动态代理是一个高性能开源的代码生成包，采用底层的字节码技术，对指定的目标类生成一个子类，并对子类增强

spring的通知类型：环绕通知（日志记录、事务处理等）、前置通知（权限管理等）、后置返回通知（关闭流、删除临时文件等）、后置（最终）通知（释放资源）、异常通知（处理异常、记录日志等）、引入通知（修改目标类）。
	