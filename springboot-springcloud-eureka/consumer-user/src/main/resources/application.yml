server:
  port: 8002
eureka:
  instance:
    prefer-ip-address: true #注册服务时使用IP地址
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: consumer-user