spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/demo
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
#      更新或者创建数据表
      ddl-auto: update
    show-sql: true

server:
  port: 8081